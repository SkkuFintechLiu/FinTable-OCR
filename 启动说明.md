## 独立软件（打包运行）

需要在对应系统上执行打包：

- macOS：在项目根目录执行 `bash packaging/build_macos.sh`
- Windows：运行 `packaging/build_windows.bat`

不懂代码的使用方式（macOS）：

- 双击运行 `一键打包并打开（macOS）.command`
- 首次运行会自动安装依赖并打包，完成后会自动打开 `DebtExtractor.app`
- 若提示“没有正确的访问权限”，在终端执行：`chmod +x '一键打包并打开（macOS）.command'`，再双击运行

打包产物：

- Windows：`dist/DebtExtractor/DebtExtractor.exe`
- macOS：`dist/DebtExtractor.app`

运行方式：

- Windows：双击 `DebtExtractor.exe`，会自动打开本地页面
- macOS：双击 `DebtExtractor.app`，会自动打开本地页面

缓存清理：

- 运行时上传目录为软件同目录的 `runtime_uploads/`
- 关闭软件后会自动清空 `runtime_uploads/` 内的文件
- 项目仓库中的 `uploads/` 目录不会被自动删除（避免误删样本）
